# 本番Supabase DBに接続するための設定
# 使用方法: このファイルを .env.production.local にコピーして使用

# SupabaseのSettings > Database > Connection stringから取得
# "Transaction"モードの接続文字列を使用（pgBouncerポート6543ではなく、直接接続ポート5432）
DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres"

# 本番環境のNextAuth設定
NEXTAUTH_URL="https://health-hub-eight.vercel.app"
NEXTAUTH_SECRET="[本番環境と同じシークレット]"

# Google OAuth（本番環境と同じ）
GOOGLE_CLIENT_ID="[本番環境と同じ]"
GOOGLE_CLIENT_SECRET="[本番環境と同じ]"

# Google AI API（本番環境と同じ）
GOOGLE_API_KEY="[本番環境と同じ]"

# Supabase設定（本番環境と同じ）
NEXT_PUBLIC_SUPABASE_URL="[本番環境と同じ]"
NEXT_PUBLIC_SUPABASE_ANON_KEY="[本番環境と同じ]"
SUPABASE_SERVICE_ROLE_KEY="[本番環境と同じ]"
